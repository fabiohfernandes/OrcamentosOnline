# Railway Environment Variables Template
# Copy this file and set the values in Railway dashboard

# ============================================================================
# REQUIRED SECRETS - Set these in Railway dashboard
# ============================================================================

# PostgreSQL Configuration
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Redis Configuration
REDIS_PASSWORD=your_secure_redis_password_here

# JWT Authentication
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_here

# ============================================================================
# AUTO-CONFIGURED BY RAILWAY - Do not set manually
# ============================================================================

# Database Connection (automatically generated by Railway)
# DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/orcamentos_prod

# Redis Connection (automatically generated by Railway)
# REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379

# Service URLs (automatically set by Railway)
# RAILWAY_PUBLIC_DOMAIN=your-service.railway.app
# NEXT_PUBLIC_API_URL=https://${api.RAILWAY_PUBLIC_DOMAIN}
# CORS_ORIGIN=https://${frontend.RAILWAY_PUBLIC_DOMAIN}

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Custom Domain Configuration (if using custom domains)
# CUSTOM_API_DOMAIN=api.your-domain.com
# CUSTOM_FRONTEND_DOMAIN=app.your-domain.com

# Email Service (if implementing notifications)
# EMAIL_SERVICE=sendgrid
# EMAIL_API_KEY=your_sendgrid_api_key
# EMAIL_FROM=noreply@your-domain.com

# File Upload Configuration
# MAX_FILE_SIZE=10485760
# ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf,doc,docx

# Logging Configuration
# LOG_LEVEL=info
# LOG_FORMAT=json

# Performance Configuration
# NODE_OPTIONS=--max-old-space-size=2048
# MAX_CONNECTIONS=100

# ============================================================================
# SECURITY NOTES
# ============================================================================

# 1. All passwords should be at least 16 characters long
# 2. JWT_SECRET must be at least 32 characters and cryptographically secure
# 3. Never commit actual values to version control
# 4. Use Railway's environment variable management for secure storage
# 5. Rotate secrets regularly for enhanced security

# Generate secure values using:
# - Password: openssl rand -base64 24
# - JWT Secret: openssl rand -base64 32